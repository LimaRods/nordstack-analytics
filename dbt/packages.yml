packages:
  # Used for unique_combination_of_columns, which asserts the grain of fct_mrr.
  # dbt's built-in `unique` only covers a single column.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
